# Legacy modular frontend (unused)

The live SubStudy UI is **`../index.html`**, served by FastAPI at `http://localhost:8000/`.

The TypeScript/React files in this directory (`App.tsx`, `Sidebar.tsx`, etc.) are an earlier prototype and are **not loaded** by the running application. Do not edit them unless you plan to migrate back to a bundled frontend in Phase 2.
